# 更新日志

本项目所有值得记录的变更均会收录在此文件中。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## \[v0.21.12] - 2026-09-02

### 修复

- **修复安装后打开软件显示官网下载页而非编辑器的问题**：此前主窗口加载的是根目录 `index.html`（该文件同时是官网下载页发布源），v0.21.11 起 `index.html` 被下载页模板占用，导致安装版 / 便携版打开后窗口内容变成 `https://stutasliu.github.io/LNote/` 的下载页面而非本地编辑器。本次将主窗口入口解耦为独立的 `app.html`（编辑器壳，从历史版本恢复），`main.py` 主窗口改为加载 `app.html`，PyInstaller 打包清单（`Inkpad.spec` / `InkpadDebug.spec`）同步改为打包 `app.html`；官网 `index.html` 下载页保持不动，两者互不干扰。

### 测试

- 前端构建通过（`node tools/build-app.js` + `npx vite build`，dist-web 同步更新）；Python 语法检查通过。

- 单元测试通过（`npx vitest run tests/unit`，87 项）。

- PyInstaller 打包成功（`dist\L.Note.exe`）；用户本地手动打开新 exe 验证：打开后显示编辑器界面而非网站页面。

## \[v0.21.11] - 2026-09-02

### 新增

- **思维导图右键菜单**：右键节点弹出上下文菜单（添加子节点 / 添加同级 / 编辑 / 删除 / 复制 / 剪切 / 粘贴 / 折叠展开），菜单外点击自动关闭；空白处右键弹出简化菜单（添加根子节点 / 粘贴）。

- **思维导图复制 / 剪切 / 粘贴**：支持 Ctrl+C / Ctrl+X / Ctrl+V 复制粘贴节点子树，剪贴板同时写入 JSON（跨文档粘贴保留结构）和纯文本（缩进层级 Markdown），支持跨 mind 实例粘贴。

- **思维导图样式面板升级（节点 / 页面双 Tab）**：

  - 节点 Tab：填充 / 边框 / 文字 23 色色板，形状切换（矩形 / 圆角 / 椭圆 / 菱形 / 六边形），线宽 / 线型 / 圆角，文字对齐（左 / 中 / 右），加粗 / 斜体 / 下划线 / 删除线，字号滑杆。

  - 连线样式：当前节点到父节点的连线可设置粗细 / 线型（实线 / 虚线 / 点线）/ 箭头开关，SVG marker 自动生成。

  - 页面 Tab：布局密度（紧凑 / 适中 / 宽松）、编号样式（无 / 1.2.3 / A.a.c / Ⅰ.Ⅱ / 一.二.三）、12 套预设主题风格（经典 / 商务 / 彩虹 / 深色 / 亮色 / 田园 / 海洋 / 日落 / 森林 / 水墨 / 樱花 / 糖果），主题网格预览卡片。

- **思维导图拖拽交互**：

  - 鼠标按住中心主题（根节点）拖动可平移整个画布（transform translate，grab 光标提示）。

  - 鼠标按住非根节点拖动可重新排序到兄弟节点之间，拖动时显示半透明幽灵元素跟随鼠标 + 蓝色插入指示线标记目标位置，松手完成排序（支持 Ctrl+Z 撤销）。

- **思维导图聚焦模式**：Ctrl+\` 切换聚焦模式，仅高亮从根到选中节点的路径及其后代，其余节点变暗。

- **思维导图 Shift+Tab**：将选中节点提升为父节点的同级（变为祖父节点的子节点）。

- **思维导图批量删除子孙**：Ctrl+Delete / Ctrl+Backspace 一键删除选中节点的所有子孙节点。

- **单实例运行时接力（「打开方式」直达已运行实例）**：主编辑器启动后在 `127.0.0.1:47331` 后台监听 IPC（socket + 4 字节长度前缀 JSON）；再次通过文件关联 /「打开方式」/ 命令行传文档启动时，若已有实例在运行，不再创建第二个主窗口，而是把文档路径转发给已运行实例，由前端 `window.__inkpadOpenExternalFiles` 处理器在**当前窗口**打开（复用磁盘文件打开链路）。前端初始化完成前到达的文档先入队，`frontend_ready()` 就绪握手后自动冲刷打开；接力成功后自动把主窗口还原到前台（Windows：还原最小化 + `SetForegroundWindow`）。图片文件仍走独立图片编辑窗口流程，不参与接力。

- **Windows 安装版（Inno Setup 6）**：新增 `installer/LNote.iss` 安装脚本，`_build_exe.bat` 在 PyInstaller 打包成功后自动调用 ISCC 生成安装包（`release\L.Note-setup-v0.21.11.exe`，约 18.3 MB）。安装向导默认安装到 `%LOCALAPPDATA%\Programs\L.Note`，提供桌面 / 开始菜单快捷方式，可选注册 `.md/.markdown` 与常见图片文件关联，含卸载程序；`download.html` / `index.html` / `RELEASE-NOTES.md` / `SHA256SUMS.txt` 同步新增安装版下载入口与校验和。

### 测试

- 前端构建通过（`node tools/build-app.js` + `npx vite build`，dist-web 同步更新）；Python 语法检查通过；PyInstaller 打包成功（`dist\L.Note.exe`）。

- 单元测试通过（`npx vitest run tests/unit`，87 项）。

- Inno Setup 6.7.3 编译安装包通过（`release\L.Note-setup-v0.21.11.exe`）；安装 / 卸载冒烟验证通过（默认安装路径、快捷方式、卸载清理、可选文件关联）。

## \[v0.21.10] - 2026-09-01

### 新增

- **「关于 L.Note」弹窗**：顶栏「更多」菜单新增「关于 L.Note」入口，弹出关于弹窗，包含三部分能力：

  - **显示版本号**：打开弹窗时从后端读取当前版本号（`get_version` API），浏览器环境回退到本地常量。

  - **检查更新**：点击「检查更新」通过 Python 后端联网查询最新版本（`check_update` API，优先 GitHub Releases API，失败自动回退 Gitee API），实时显示「已是最新版本 / 发现新版本 / 无法连接更新服务器」状态；发现新版本时同步弹出轻提示。

  - **更新日志链接**：「更新日志」「项目主页」按钮调用系统默认浏览器打开 GitHub 发布页 / 仓库主页（`open_external` API），避免 WebView 内嵌 iframe 的兼容性问题。

- 后端新增 `_version_greater()` 版本比较函数：支持 `v0.21.9` / `0.21.9` 两种格式，按 (major, minor, patch) 三元组比较。

### 测试

- 前端构建通过（`node tools/build-app.js` + `npx vite build`，dist-web 同步更新）；Python 语法检查通过；PyInstaller 打包成功（`dist\L.Note.exe`）。

- 单元测试通过（`npx vitest run tests/unit`，87 项）。

- `_version_greater` 版本比较 8 组用例（含 `v` 前缀、跨段比较、空串）与 `check_update` 三种场景 mock 测试（GitHub 成功 / Gitee 回退 / 全部失败）均通过。

## \[v0.21.9] - 2026-09-01

### 新增

- **流程图「泳道（泳池）」**：支持水平 / 纵向双向泳道；工具栏新增「水平泳道 / 纵向泳道 / 双泳道（一次一对）/ 分割线（沿中线一分为二）」按钮；点击泳道标题直接重命名；拖动标题栏整条移动（内部节点跟随），拖动底部 / 右侧边缘调整尺寸，拖动时自动消除泳道重叠（被挤开的泳道连带内部节点整体让位）；Delete 键删除选中泳道；样式设置面板支持泳道底色 / 标题栏 / 边框 / 标题文字颜色。

- **流程图交互增强（仿 ProcessOn）**：

  - 空白处拖拽框选多个节点，可整组拖动 / 批量删除；样式设置支持多选批量修改。

  - 节点边缘连接点拖出连线（hover 节点显示连接点，拖到目标节点即建线）；选中连线后两端出现连接点，可拖动端点重新连接。

  - 连线形状切换：直线 / 曲线 / 直角折线（工具栏按钮，选中连线后自动同步高亮）；连线自动绕开其他节点（正交绕障路径）。

  - `Ctrl+Z` 撤销：节点 / 泳道增删、移动、连线、样式调整等操作均可逐步撤销。

- **流程图样式设置面板**：流程图文档右上角新增「样式设置」按钮（与文档信息按钮并列，仅流程图显示，其他格式不出现）。选中节点可调填充 / 边框 / 文字颜色、线宽、线型（实线/虚线/点线）、圆角、加粗、斜体、字号；选中连线可调线条 / 文字颜色、线宽、线型、箭头开关、字号；选中泳道可调底色 / 标题栏 / 边框 / 标题文字颜色。色板全部按钮化（23 色），避免原生取色器在桌面 WebView 不可用；样式实时预览并写入文档持久化。

- **样式设置按钮 hover 提示**：鼠标悬停显示「样式设置」浮层提示（含小箭头），移出自动消失。

### 修复

- **样式设置面板无法关闭**：面板此前只能靠再次点击按钮关闭，点击画布空白 / 工具栏等外部区域时面板保持打开、空状态提示持续显示。修复：点击面板外部任意位置自动关闭面板并移除按钮激活态；点击节点 / 连线 / 泳道时面板保留，便于选中后继续调样式。

- **颜色设置不生效**：修复 SVG 内联样式与样式表优先级冲突，节点 / 连线 / 泳道颜色修改即时生效。

### 变更

- **可视化区右键拦截**：流程图 / 思维导图 / 思维笔记画布内右键不再弹出纯文本编辑器菜单，避免误操作。

### 测试

- 前端构建通过（`node tools/build-app.js` + `npx vite build`，dist-web 同步更新）；Python 语法检查通过；PyInstaller 打包成功（`dist\L.Note.exe`）。

- 浏览器端到端验证通过：样式面板开关（按钮开关 / 点击外部关闭 / 点击节点保留 / 点击面板内部保留）、节点 / 连线 / 泳道样式修改实时生效并持久化、hover 提示显示与消失、泳道添加 / 重命名 / 拖动 / 分割 / 删除、框选多选与整组拖动、连线拖拽建立与端点重连、连线形状切换、Ctrl+Z 撤销均符合预期。

## \[v0.21.8] - 2026-08-27

### 新增

- **设置面板**：顶栏「更多」菜单新增「设置」入口，弹出双 Tab 设置弹窗（通用 / 快捷键）。通用页支持编辑器字体大小（滑块 10–26px，实时预览，与 Ctrl+滚轮缩放共用同一存储）、行号显示开关、自动换行开关；所有设置持久化到 localStorage（`inkpad.settings.v1`），「恢复默认设置」一键还原。原 `inkpad.fontsize` 旧键自动迁移。

- **快捷键自定义**：快捷键页列出全部 17 项可配置快捷键（保存 / 新建 / 新建富文档 / 新建流程图 / 新建思维导图 / 新建思维笔记 / 新建便利贴 / 查找 / 替换 / 查找上下一个 / 格式化 / 注释 / 折叠 / 展开 / 选中下一处 / 合并行），点击右侧按键按钮进入录制态（按 Esc 取消），录制要求至少包含一个修饰键，冲突检测包含 Ctrl/Cmd 别名（如 `Ctrl-F` 与 `Cmd-F` 视为占用）；支持 `Ctrl/Cmd` 双平台映射——Windows 主修饰键为 Ctrl、macOS 为 ⌘，CodeMirror 键表自动生成互补变体，保证跨平台生效。

- **新建文档类型快捷键**：为各文档类型提供独立的一键新建全局快捷键——新建富文档 `Ctrl+Shift+N`、新建流程图 `Ctrl+Alt+N`、新建思维导图 `Ctrl+Alt+M`、新建思维笔记 `Ctrl+Shift+M`、新建便利贴 `Ctrl+Alt+S`，与原有「新建」纯文本文档（`Ctrl+N`）并列；全部 5 项均可到设置弹窗「快捷键」页重新录制绑定，命中后直接创建对应类型文档，便利贴会自动切换视图并弹出「已新建便利贴」提示。

### 变更

- **快捷键体系重构**：编辑器 `extraKeys` 由硬编码改为按 `settingsState.shortcuts` 动态生成（`buildCmExtraKeys`）；全局保存 / 新建快捷键由固定 `(e.ctrlKey || e.metaKey)` 改为统一走 `handleGlobalKeydown` + `matchesCombo` 组合键匹配，修饰键顺序无关且与录制格式一致。

### 测试

- `node --check` 语法校验通过；快捷键核心逻辑（组合键解析 / 匹配 / CM 键表生成 / 冲突检测）36 项断言全部通过。

- 浏览器端到端验证通过：菜单 → 设置弹窗开关、双 Tab 切换、17 项快捷键列表渲染、字号滑块实时生效并持久化、行号 / 自动换行开关、按键录制（含无修饰键拒绝、冲突检测、Esc 取消）、恢复默认设置均符合预期；全局快捷键 Ctrl+S / Ctrl+N 命中后正确触发 `saveDoc` / `newDoc`（事件 `preventDefault` 探针验证），未分配组合（Ctrl+Alt+Q）与编辑器级快捷键（F3）不被窗口级拦截。

- 新建文档类型快捷键端到端验证通过：`Ctrl+Shift+N` / `Ctrl+Alt+N` / `Ctrl+Alt+M` / `Ctrl+Shift+M` / `Ctrl+Alt+S` 命中后分别创建富文档 / 流程图 / 思维导图 / 思维笔记 / 便利贴，新建便利贴自动切换便利贴视图并弹出「已新建便利贴」提示。

## \[v0.21.7] - 2026-08-26

### 新增

- **顶部格式工具栏按文档类型显隐**：B/I/U/删除线/行内代码/引用/列表/表格 格式工具栏（`#abToolbar`）仅对富文本文档生效，调整为仅在打开富文本文档时显示，打开其他格式（纯文本 / 流程图 / 思维导图 / 思维笔记）自动隐藏；应用启动时默认隐藏，避免非富文本文档闪现。

- **文档地图升级为「可滚动文档地图」**：全文档按真实行距渲染文本缩略（不再区分布局外色块），内容超过面板高度时地图内部可滚动；视口指示器随编辑器滚动同步移动并自动带动地图滚动条，拖动地图滚动条 / 滚轮反向跳转编辑器（双向联动）；canvas 只绘制可见切片（始终 1:1 原生分辨率），超大文档文字清晰不卡顿。

### 修复

- **顶栏右侧按钮左移**：格式工具栏隐藏后其 `margin: 0 auto` 居中布局失效，导致右侧功能按钮（搜索 / 信息面板 / 保存等）整体左移。修复：右侧操作区 `.ab-actions` 增加 `margin-left: auto` 始终吸附最右端；格式工具栏改为绝对居中定位，与右侧按钮互不干扰（显示 / 隐藏两种状态均正确）。

- **富文本文档打开时格式工具栏不显示**：`els` 映射中工具栏 id 误写为 `ab-toolbar`（真实 id 为 `abToolbar`），`getElementById` 返回 `null` 且被守卫条件静默跳过。修正 id 映射后恢复正常显隐。

### 变更

- 工程规范：`.trae/rules/project_rules.md` 新增「迭代开发防回归规范」（修改前核实 / 修改中通盘 / 修改后完整验证）与逐项代码检查清单；`.gitignore` 忽略临时诊断与测试产物（`_test/`、`.edge-cdp-profile/`、`tests/e2e/_*`）。

### 测试

- 前端构建通过（`node tools/build-app.js` + `npx vite build`，dist-web 同步更新）；Python 语法检查通过；单元测试全量通过（87 项）。

## \[v0.21.6] - 2026-08-24

### 变更

- **文档地图升级为「真实文本缩略图」**：视口内区域改为按行渲染文档真实文字（迷你字号直接绘制字符，可辨识段落形状、标题与内容密度），不再只是抽象色块；视口外区域仍保留行长度色块。文字与视口框严格对齐，点击 / 拖拽跳转逻辑不变。

  - 行距自适应容器高度（3–6px），坐标 = 行号 × 行距，滚动 / 跳转后文字带与高亮视口框完全重合。

  - 渲染按可见行区间裁剪（`lineAtHeight` 折叠感知），超大文档首屏不卡顿。

  - 测试：CDP 真实浏览器验证——150 行文档文字像素严格落在视口带内、滚动后文字带随视口框同步移动、点击小地图按比例跳转准确。

### 测试

- 前端构建通过（`node tools/build-app.js` + `npx vite build`，dist-web 同步更新）；Python 语法检查通过；单元测试全量通过（87 项）；CDP E2E 验证通过。

## \[v0.21.5] - 2026-08-24

### 新增

- **PDF 查看器**：文件树双击 /「打开方式」/ 导入菜单可直接打开 `.pdf`。基于 pdf.js 懒加载渲染（首次打开才加载，离线携带 cmaps 与标准字体，中文 CID 不乱码），支持翻页（◀/▶）、缩放（＋－/Ctrl+滚轮/重置/适应宽度）、文本层选中复制、空白页检测告警；提供「提取为文本」（跨页抽取转 Markdown 新文档）与「复制内容」（全文到剪贴板）；PDF 以只读方式登记进侧栏（`kind=pdf`，保存/导出/磁盘写回全链路拦截）。

- **Word 文档查看与导入**：双击 `.doc/.docx` 打开模态查看器——`.docx` 经 mammoth 渲染为排版富文本并统计字数，旧版 `.doc` 由内置 OLE2 piece table / RTF 控制字 / ZIP-OOXML / HTML / 纯文本多格式解析引擎提取文本；支持一键「导入富文档」（行文本转块编辑器）、「导入 Markdown」与「复制内容」；`kind=doc` 条目复用磁盘路径避免重复，全程只读。

- **文档地图（小地图）**：编辑器右侧新增仿 VS Code 的 130px 代码小地图（Canvas 按行采样绘制全景缩略图，行长度映射色块宽度，采样步长自动适配避免全行遍历卡顿）；高亮视口框随滚动实时同步，点击 / 拖拽按比例跳转（点击点置于视口中线）；顶栏开关状态持久化到 localStorage（`inkpad.docmap.v1`），仅文本文档显示，非文本自动隐藏。

- **图片查看器升级为图片编辑器**：通过文件关联双击 /「打开方式」传入图片直接进入独立图片编辑窗口；新增完整编辑链——旋转 90°、水平/垂直翻转、裁剪框拖拽（四角手柄）、尺寸调整（锁定宽高比 + 高质量重采样 + unsharp 锐化）、滤镜（亮度/对比度/饱和度滑杆 + 灰度/反色）；支持撤销/重置、覆盖保存、另存为、窗口缩放，退出可放弃修改。

- **文件关联注册脚本**：新增 `register-file-assoc.ps1`（`.bat` 仅为包装），仅写 HKCU（无需管理员权限）将 `dist\L.Note.exe` 注册为常见图片格式与 `.md/.markdown` 的默认打开程序，并在右键「打开方式」中登记 L.Note。

- **JSON 结构折叠**：新增 `json-brace` 折叠（自动跳过字符串/注释配对，单行紧凑 JSON 也可折叠，数组折叠显示元素数「⋯ N 项」）与 `json-auto` 全局兜底（首非空字符为 `{`/`[` 即启用折叠）。

- **块编辑器图片增强**：图片块支持选中后「更换图片」、插入图片块直接弹出选择器；图片加载失败自动经 `read_file_b64` 以 data URI 重试；新增富文档目录缓存（`getCachedRichDir/setCachedRichDir`）加速图片解析。

- **后端新增能力**：新增 `get_pending_open_file`（「打开方式」传入非图片文件时启动自动打开并防重入）、`debug_log`、`read_file_b64`、`read_doc_text`（旧版 `.doc` 文本提取）；ImvApi 新增 `save_image_file/save_image_as/close_window/resize_window`；启动分流重构——图片参数直接进图片编辑器，非图片文件记入待打开队列，否则按原逻辑起主窗口。

### 变更

- **启动性能优化**：Mermaid（约 2.5MB）、PDF.js、mammoth 三个大库从启动 bundle 拆分为独立懒加载包（`vendor-mermaid.js` / `vendor-pdf.js` / `vendor-doc.js`），首次使用时才动态注入，启动时间大幅缩短。

- **启动首文档决策重构**：`openPendingExternal` 最先调度并防重入、等待 pywebviewready 重试，有外部传入文件时跳过默认文档，避免启动瞬间默认文档闪现。

- **折叠命令兼容修复**：`foldAll/unfoldAll` 改用 `execCommand`、`foldToLevel` 改用 `CodeMirror.fold.auto`，兼容新版 helper 注册方式。

- **PDF/Word 全链路只读保护**：保存 / 导出 / 磁盘写回 / 复制文档对 PDF/Word 条目统一拦截。

### 测试

- 前端构建通过（`node tools/build-app.js` + `npx vite build`，dist-web 同步更新）；Python 语法检查通过；单元测试全量通过（87 项）。

## \[v0.21.4] - 2026-08-18

### 新增

- **「打开所在文件夹」功能**：在文档功能列表中新增「打开所在文件夹」，可在系统文件管理器中定位文档保存的文件夹。

  - 入口一：AppBar 功能菜单（更多）新增「打开所在文件夹」，作用于当前打开的文档。

  - 入口二：文档列表三点菜单新增「打开所在文件夹」，作用于对应文档。

  - 桌面版通过 `show_in_folder` 桥接：Windows 用资源管理器打开并选中文件（`explorer /select,`），macOS 用 Finder 显示（`open -R`），其他平台打开所在目录。

  - 应用本地（localStorage）文档未关联磁盘文件时给出明确提示；纯网页环境提示需在桌面版使用。

  - 测试：新增 `revealTarget` 路径判定单元测试（4 项），全部通过（共 87 项）。

## \[v0.21.3] - 2026-08-18

### 修复

- **从 Excel/WPS 复制内容粘贴时被误识别为图片**

  - **问题原因**：Excel/WPS 复制选区时，剪贴板会同时携带两种数据——文本（Tab 分隔的表格数据）和图片（选中区域被渲染成图片）。原粘贴逻辑只要检测到 `image/` 类型的剪贴板项就 `preventDefault` 并按图片插入，忽略了文本，导致从 Excel 复制的内容被识别成图片。

  - **修复内容**：粘贴改为**文本优先**。新增 `hasClipboardText` 判断：剪贴板携带非空文本（`text/plain` 或 `text/html` 富文本）时，直接放行走默认文本粘贴；仅当剪贴板完全不含文本、只有纯图片时，才按图片路径插入。

  - **影响范围**：仅粘贴行为，不改变复制/拖拽等其他功能。

  - **测试**：新增 `hasClipboardText` 单元测试（7 项），覆盖纯文本、Excel Tab 表格文本、HTML 富文本、空白文本、异常输入等场景，全部通过。

## \[v0.21] - 2026-08-18

### 新增

- **选中文本右键菜单「翻译」功能**

  - 在编辑器中选中文本，右键菜单新增「翻译」项，点击后在弹窗中展示译文。

  - 自动检测语言并中英互译（`detectLang` + `targetFor`）。

  - 桌面版通过 worker 线程 + Python 桥接（pywebview）执行翻译，界面不卡顿；翻译失败时给出回退提示。

  - 支持关闭弹窗并返回编辑器。

  - 测试：新增翻译模块单元测试（11 项）与 CDP E2E 验证。

### 修复

- JSON 格式切换逻辑优化：仅当全文整体是合法 JSON 时才切换语言，混合内容保持原语言（v0.21.2）。

