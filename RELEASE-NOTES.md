# L.Note v1.0.3 发布说明

> L.Note本地笔记编辑软件：语法高亮 / Markdown 实时预览 / Mermaid 图表 / JSON·XML 格式化。
> 纯本地运行，数据保存在本地存储，无需联网、无账号。

## 下载

> 官方 Windows 安装版（标准安装向导 + 可选文件关联 + 卸载程序，自 v0.21.11 起提供）与便携版均可直接下载，推荐大多数用户下载安装版。本版 v1.0.3 新增 Ctrl+J 聚焦侧栏搜索框、全窗口预览时隐藏语言选择行，并修复「查找 / 替换」框按 Enter 误把正文回车掉；上一版 v1.0.2 新增「预览显示方式」设置项（全窗口 / 左右分栏），并修复预览区鼠标光标消失与全窗口预览铺满范围；上一版 v1.0.1 重做了全套文档类型图标：新建菜单、侧栏文档列表、顶栏面包屑、富文档浮动工具条与右键「AI 图表」菜单里原先的 emoji 图标，全部替换为统一线性风格的 SVG 图标，并按文档类型各赋一种颜色——文档（灰蓝）/ 富文档（紫）/ Word（深蓝）/ 思维笔记（品红）/ 便利贴（琥珀）/ 图表（青）/ 流程图（蓝）/ 思维导图（绿）/ PDF（红），「打开文件夹」「导入文件」两个菜单动作图标也一并换为线性图标并上色（青绿 / 橙棕）；深色模式下自动切换为同色相的提亮值，回收站与 AI 菜单禁用态则统一置灰。同时修复自动更新安装期间屏幕无任何反馈的问题：守护进程调用安装器由 `/VERYSILENT` 改为 `/SILENT`，保留原生安装进度窗口。上一版 v0.23.2 新增 AI 能力并支持文档列表拖动排序：在「设置 → AI 助手」填入自己的 API Key（OpenAI 兼容接口，默认 DeepSeek）后，在正文中选中文本即可通过右键菜单或浮动工具条调用 AI 助手（润色 / 续写 / 总结要点 / 扩写 / 纠错 / 生成大纲，结果在独立浮层流式输出，可随时停止并替换原文或插入到光标处），并可一键生成流程图 / 思维导图；密钥仅保存在本机配置文件，不写入任何文档、不上传第三方。再上一版 v0.21.17 已将自动更新改为守护进程原地静默升级并自动重启，自 v0.21.13 起支持自动更新。

- 安装版（推荐，约 18.3 MB）：`L.Note-setup-v1.0.3.exe`

  - 标准 Inno Setup 安装向导，自定义安装目录（默认 `%LOCALAPPDATA%\Programs\L.Note`），自动创建开始菜单快捷方式

  - 可选安装「注册文件关联」：勾选后 `.md` / `.markdown` / 常见图片格式双击默认用 L.Note 打开（仅写 HKCU，无需管理员权限）

  - 自带标准卸载程序：Windows 设置 → 应用 → 已安装的应用 → L.Note → 卸载，或开始菜单「卸载 L.Note」

- 便携版（免安装，约 16.7 MB）：`L.Note-v1.0.3-win64.exe`

- 校验和：见 `SHA256SUMS.txt`（可用 `certutil -hashfile <文件> SHA256` 核对）

> 系统要求：Windows 10/11（需已安装 Edge WebView2 运行时，Win11 自带；Win10 一般已自动安装，缺失时首次启动会提示安装）。

## 使用说明

1. 安装版：双击 `L.Note-setup-v1.0.3.exe` 进入安装向导 → 在「选择附加任务」页按需勾选「创建桌面快捷方式」或「文件关联」（后者把 L.Note 设为 .md/.markdown 与常见图片的默认打开程序）→ 依次完成「准备安装」「正在安装」→ 在完成页点击「完成」后从开始菜单或桌面快捷方式启动 L.Note。
2. 便携版：双击 `L.Note-v1.0.3-win64.exe` 直接运行，无需安装；想注册文件关联时可运行仓库内 `register-file-assoc.ps1`（仅写 HKCU，无需管理员权限）。
3. 未签名程序首次运行可能出现 SmartScreen「未知发布者」提示：点击「更多信息」→「仍要运行」即可（详见下方「关于签名」）。
4. 数据保存在本机（浏览器 localStorage），换电脑/清理数据前请先用「导出」备份。

## 关于签名

当前发布包未做代码签名（无受信任 CA 证书），Windows SmartScreen 会提示「未知发布者」。这不影响功能与安全；如需消除提示，可购买 OV/EV 代码签名证书（约 ¥1000/年）后重新签名发布，后续版本可跟进。

## 更新日志（本版亮点）

**v1.0.3**

- **新增 Ctrl+J 聚焦侧栏搜索框**：侧栏文档搜索框新增内置快捷键 `Ctrl+J`，按下即聚焦搜索框并全选原有内容；若侧栏处于折叠状态会先自动展开，随后可直接输入关键词筛选文档，无需鼠标点击。该快捷键为内置固定绑定，不占用「设置 → 快捷键」中可自定义的快捷键表，同时移除了搜索框上冗余的「Ctrl J」提示气泡与对应样式。

- **全窗口预览时隐藏语言选择行**：全窗口预览以右侧编辑区容器为参照铺满，此前顶部的「语言选择」工具行仍占据一行高度；现改为全屏预览时自动隐藏该行，把纵向空间让给预览内容，切回「左右分栏」或关闭预览时自动恢复显示。

- **修复查找框按回车误把正文回车掉**：此前在「查找 / 替换」浮层的查找框中按 Enter，虽已阻止默认行为，但查找函数仍会同步把焦点抢回编辑器，导致同一次按键的后续事件投递到编辑器、在文档中插入换行；现改为当焦点仍停留在查找 / 替换输入框内时不抢焦点，Enter 只执行「查找下一个」，正文内容不受影响。

**v1.0.2**

- **新增「预览显示方式」设置项**：设置 → 通用新增「预览显示方式」，可选「全窗口」与「左右分栏」，默认「全窗口」——打开预览时铺满右侧编辑区，阅读版面最大化，左栏文档列表与顶栏保持可见；选择「左右分栏」则回到原有的左右并排 + 可拖动分隔线布局。该设置随设置项持久化到本地，重启后保持。

- **修复预览区鼠标光标消失、文字无法选中**：根因是 `#preview-body` 无条件设置了抓手光标（`cursor: grab`）并禁用文本选择（`user-select: none`），本意只服务 Mermaid 图表的拖拽平移，却把 Markdown / HTML 预览的文字光标与选中复制能力一起禁掉了；现改为按预览类型区分——仅图表预览保留抓手光标与拖拽平移，Markdown / HTML 预览恢复正常文本光标，可正常选中复制文字。

- **修正全窗口预览的铺满范围**：上一版全窗口预览以整个软件窗口为参照定位，会把左侧文档列表栏一并盖住；现改为以「右侧编辑区容器」为参照铺满，预览只占满编辑区，左侧文档列表栏与顶栏始终可见，同时切换「全窗口 / 左右分栏」时不再残留上一次拖动留下的宽度。

**v1.0.1**

- **修复富文本文档点击「保存」提示保存失败**：根因是「另存为」原生对话框的文件类型过滤器描述含品牌名「L.Note」中的点号，pywebview 的 `parse_file_type` 只接受 `[\w ]+ (*.ext)` 形式并会抛 `ValueError`，而该异常在平台 try 之外，直接导致整个保存流程失败；现前端过滤器描述改为「LNote 富文档 (*.json)」，Python 侧 `_ask_save_path` 对每条过滤器逐条校验、剔除非法项并在全部非法时回落到「所有文件 (*.*)」，双保险后保存恢复正常。

- **新建文档默认名称统一为「无标题 / 无标题 2 / 无标题 3 …」**：此前普通文档走「无标题」，而快捷键 `Ctrl+N` 及富文本 / 流程图 / 思维导图 / 笔记等入口走「未命名文档 N」，两套命名规则并存；现全部新建入口统一复用 `nextAutoTitle()`，按现有「无标题」文档数量追加序号，侧栏与标题框显示一致。

- **新建文档不再自动写入磁盘**：新建文档仅保存在本地存储，必须由用户显式点击「保存 / 另存为」才创建磁盘文件，此后编辑才会自动同步到磁盘；修复「新建的文档未经确认就落盘」的问题，未关联磁盘文件时状态栏显示「已保存」而非「已保存到磁盘」。

- **预览内容安全加固**：Markdown / HTML 预览挂载前新增「标签 + 属性」白名单净化（丢弃 `<script>` / `<iframe>` / `<object>` / 表单控件等，仅保留 `href` / `src` 等必要属性并校验 URL 协议，拦截 `javascript:` 等危险链接）；Mermaid 安全级别由 `loose` 收紧为 `strict`；`app.html` 新增 Content-Security-Policy，并把 HTML 预览 iframe 的 `sandbox` 收紧为仅 `allow-scripts`（去掉 `allow-same-origin`），杜绝预览内容逃逸。

- **高危原生 API 增加会话令牌校验**：Python 端对写盘、移动 / 删除文件、保存图片、执行更新安装包等高危方法要求首位参数携带本进程随机生成的令牌（`hmac.compare_digest` 恒定时间比对），令牌仅保存在前端模块作用域、页面内注入脚本无法读取，因而无法直接调用 `window.pywebview.api` 触发高危操作；前端所有高危调用统一改走 `callApi()` 自动前置令牌。

- **自动更新增加 SHA256 完整性校验**：下载完成后先进入「正在校验安装包完整性」状态，Python 端按 Gitee raw → GitHub raw → GitHub Pages 顺序拉取官方 `SHA256SUMS.txt`，与已下载临时文件的实际 SHA256 比对，一致才 `os.replace` 就位；校验和不匹配或无法获取官方值时删除临时文件并中止安装，防止下载被篡改或损坏。

**v1.0.0**

- **正式版本号转正 V1.0.0**：代码（`main.py` / `package.json` / 关于面板）、安装包、发布页、README 与软著材料（源程序 PDF 页眉、操作说明书登记版本号）统一对齐到 V1.0.0。

- **品牌标识全面更名 Inkpad → L.Note**：源码中残留的旧品牌字样统一改为 L.Note——前端全局对象 `InkpadApi` → `LNoteApi`，Python 侧私有属性 `__inkpad*` → `__lnote*`；构建配置改名为 `LNote.spec` / `LNoteMac.spec` / `LNoteDebug.spec`，Vite 插件改名为 `tools/vite-plugin-lnote.js`，样式表由 `css/inkpad-rich.css` 改为 `css/lnote-rich.css`，界面文案与文档不再出现旧品牌。

- **旧版富文档目录一次性平滑迁移**：富文档自动存储目录由旧名改为 `L.NoteRich`；首次使用时若检测到旧的 `InkpadRich` 目录，会自动同盘 rename 迁移（O(1) 操作），迁移失败（文件被占用等）时回退继续使用旧目录，老用户数据不丢。

- **对外协议标识保持不变，升级无感**：localStorage 键（`inkpad.docs.v1` / `inkpad.active.v1` / `inkpad.content.*` / `inkpad.settings.v1` 等）、富文档标记 `<!-- inkpad:rich -->` 与剪贴板类型 `application/x-inkpad-mind` 全部保持原样，升级后既有笔记、便签、图片与剪贴板内容均照常读取。

- **软著源程序文档消除「?」占位**：`_gen_src_pdf.py` 新增符号字体回退机制——基准字体（宋体）缺字形时，自动从 Symbol / Emoji 字体中挑出覆盖率最高者并分段绘制；源程序 PDF 中不再把无法显示的字形落成问号，残留问号仅剩源码本身自带的半角问号。

- **软著材料与发布文档同步**：源程序 PDF 页眉版本号更新为 V1.0.0，操作说明书登记版本号、安装包 / 便携版文件名、发布说明、下载页与 CHANGELOG 同步到本版。

**v0.24.0**

- **全套文档类型图标改为统一线性 SVG 并全彩化**：新建菜单、左侧文档列表、顶栏面包屑、富文档浮动工具条与右键「AI 图表」菜单里原先的 emoji 图标全部替换为单色线性风格的矢量图标，并按文档类型各赋一种颜色——文档（灰蓝）/ 富文档（紫）/ Word（深蓝）/ 思维笔记（品红）/ 便利贴（琥珀）/ 图表（青）/ 流程图（蓝）/ 思维导图（绿）/ PDF（红）。

- **「打开文件夹」「导入文件」两个菜单动作图标一并上色**：同样由 emoji 换为线性 SVG，「打开文件夹」取冷色青绿、「导入文件」取暖色橙棕，与紧邻的六类文档按钮在颜色上区分开。

- **配色抽为 CSS 变量双层实现，并适配深色模式**：`css/base.css` 新增 11 个 `--ico-*` 变量与 11 条 `.ico-*` 规则，深色模式下自动切换为同色相提亮值；颜色挂在 svg 元素自身的 class 上、描边仍用 `currentColor`，从而让置灰态可以整体覆盖类型色。

- **置灰态统一覆盖类型色**：回收站条目、富文档 AI 菜单禁用项不再保留各自的类型色，统一置灰，避免与「可用 / 不可用」的视觉语义冲突。

- **图标定义集中管理**：`src-app/01-core.js` 内新增 `ICON_ATTR` / `svgIcon()` / `DOC_ICONS`，富文档浮条、文档列表、打开文档、可视化、文档操作、AI 图表等调用点统一改用矢量图标；顶栏面包屑改为以 `innerHTML` 渲染。

- **自动更新安装期间不再无任何反馈**：守护进程调用安装器由 `/VERYSILENT` 改为 `/SILENT`（含 `/NOCANCEL`），保留原生安装进度窗口，用户可直观看到安装进展（commit c483ec9）。

**v0.23.2**

- **新增 AI 助手（自备密钥 BYOK）**：在「设置 → AI 助手」填入自己的 API Key（OpenAI 兼容接口，默认 DeepSeek）即可使用；密钥仅保存在本机配置文件，不写入任何文档、不上传第三方，界面只展示脱敏信息。

- **选中即用 AI 助手**：在正文中选中文本后，文本编辑器右键菜单 / 富文档浮动工具条会出现「AI 助手」，提供润色、续写、总结要点、扩写、纠错、生成大纲六种指令；结果在独立浮层流式输出，可随时停止，并支持「替换原文 / 插入到光标处 / 复制」。

- **AI 图表生成**：选中文本后可一键生成流程图 / 思维导图；AI 只产出结构与连线，节点坐标全部由本地布局器计算，确认后另建独立文档，不覆盖原文档。

- **未配置引导与边界保护**：未配置密钥时点击 AI 入口会引导前往设置；空选区、超长文本等场景给出明确提示；同一时刻只运行一个 AI 任务，切换文档或关闭面板会自动取消。

- **左侧文档列表支持左键拖动排序**：左键按住任意文档即可上下拖动调整顺序（4 px 阈值，不干扰单击打开文档与右键菜单），排序结果本地持久化。

- **自动更新竞态修复**：修复静默更新时安装器 `[Run]` 与守卫进程重复启动新版 exe 的问题（commit 90e0c01）。

**v0.21.17**

- **自动更新改为守护进程原地静默升级并自动重启**：解决「更新下载后没有安装流程窗口弹出、要我一步步手动操作」的困扰——新版检测到更新后自动从 Gitee 静默下载新安装包，下载完成后由临时守卫进程按当前实际安装目录静默执行安装，装完自动关闭旧窗口并重启到新版，全程无需手动介入。

- **完整覆盖「检测 → 下载 → 安装 → 重启 → 结果」闭环**：守卫进程通过一次性结果文件回报安装结果，重启后前台 toast 提示「已更新到 v0.21.17」；若安装失败则明确弹出失败原因，不再出现「下载了却不知道装没装」的情况。

- **手动检查与启动自动检查统一入口**：「关于 L.Note」中的手动检查与启动时的自动检查共用同一套更新通知逻辑（notifyNewUpdate），不再产生重复弹窗/重复 toast。

**v0.21.16**

- **「关于 L.Note」菜单项新增更新提示角标**：检测到软件更新后，除右上角「更多」三点按钮外，「更多 → 关于 L.Note」行右侧也点亮红色「新」角标——打开菜单即可在「关于 L.Note」处看到高亮提示，明确告知更新操作入口所在。

- **两处角标与更新状态严格联动**：三点按钮与「关于 L.Note」项上的「新」角标由同一更新状态驱动（`setUpdBadge` 统一点亮 / 隐藏），仅在检测到新版本时显示、已是最新版自动隐藏；并配套新增 E2E 用例（headless Edge/CDP）验证两处角标同步点亮。

**v0.21.15**

- **顶栏「更多」三点菜单重构为「分组 + 就地展开」**：原先一长串命令改为收纳式分组菜单——「JSON 工具 / 转换工具 / 文本工具 / 插入块」四个大项带展开箭头，点击后在菜单内就地展开子菜单（同一时刻仅展开一个，箭头随展开状态旋转，子菜单以「大小写 / 全半角 / 删除行 / 行操作 / 折叠」分区小标题与分隔线排版）；展开其他分组时自动收起上一分组，菜单始终紧凑不溢出窗口。

- **菜单命令改为直接调用对应功能模块**：全部菜单命令（JSON 格式化 / 压缩 / 转义 / 去转义 / 复制、Unicode↔中文 / Base64 / URL 编解码 / 时间戳转换、大小写 / 全半角 / 删除行 / 行排序 / 折叠级别 / 插入代码段 / 剪贴板历史、插入代码块 / 图片 / 表格 / 流程图 / 图标 / 数学公式 / 标注 / 任务列表 / 引用 / 自动目录 / 分割线 / 分栏，以及编码转换 / 文件比较 / XML 格式化 / 切换预览 / 导出文档 / 另存为 / 打开所在文件夹 / 删除文档 / 设置 / 关于 L.Note）均改为直调对应功能模块处理函数，不再经隐藏工具栏按钮转发，命令响应更直接稳定。

- **菜单按当前文档类型自适应过滤**：打开菜单时按活动文档类型显示 / 隐藏命令——文本类文档显示「工具」分组（编码转换 / 文件比较 / XML 格式化 / JSON 工具 / 转换工具 / 文本工具）与「插入块」分组，富文档仅显示插入块等适用命令，Markdown / HTML 文档才显示「切换预览」；菜单每次打开都从合拢状态开始。

- **长菜单面板内滚动**：菜单内容超高时在面板内滚动（高度自适应窗口），不再溢出到窗口外。

**v0.21.14**

- **修复「保存 / 另存为」对话框修改名称后标题不同步**：此前在保存对话框输入新文件名仅写入磁盘路径，文档标题仍停留在旧值或「未命名文档」，侧边栏列表显示滞后。本次在保存成功后自动将文件名（去扩展名）采纳为文档标题，同步刷新当前已打开文档的标题输入框与侧边栏列表；富文档与普通 / 可视化文档两条保存分支（首次保存与另存为）均已覆盖。

- **修复右键重命名后界面不同步**：此前在「重命名」对话框改名成功后，侧边栏列表与标题输入框仍显示旧名称。本次重命名成功后立即将新标题写入当前打开文档的标题框并刷新侧边栏列表。

**v0.21.13**

- **启动自动检查更新**：软件打开后自动联网检查是否最新版本——已是最新则静默不做任何处理；发现新版本时，在右上角「更多」三点按钮内显示红色「新」更新标签，并自动弹出更新提示窗，用户可选择「稍后」（关闭提示窗不做处理）或「立即更新」。

- **一键自动更新**：点击「立即更新」后自动从 Gitee Releases 下载新版安装包（提示窗内实时显示下载进度），下载完成后静默安装（`/VERYSILENT` 不弹向导），安装完成后自动关闭旧窗口并重启到新版本。

- **右上角红色更新标签**：顶栏「更多」三点按钮内新增更新徽标（红色圆点 + 「新」字），仅当检测到新版本时显示，已是最新版则隐藏。

- **数据与隐私**：应用仅启动时自动检查一次是否有新版本（只查询版本号，不上传任何数据），或用户主动点击「关于 → 检查更新」时联网查询。

**v0.21.12**

- 修复安装后打开软件显示官网下载页而非编辑器的问题：主窗口页面与官网下载页彻底解耦（新增独立 app.html 作为主窗口页面，不再依赖官网首页），并同步更新打包清单与发布文件。

**v0.21.11**

- **思维导图右键菜单**：右键节点弹出上下文菜单（添加子节点 / 同级 / 编辑 / 删除 / 复制 / 剪切 / 粘贴 / 折叠展开），菜单外点击自动关闭；空白处右键弹出简化菜单。

- **思维导图复制 / 剪切 / 粘贴**：支持 Ctrl+C / Ctrl+X / Ctrl+V 复制粘贴节点子树，剪贴板同时写入 JSON（跨文档粘贴保留结构）和纯文本（缩进层级 Markdown），支持跨 mind 实例粘贴。

- **思维导图样式面板升级（节点 / 页面双 Tab）**：

  - 节点 Tab：填充 / 边框 / 文字 23 色色板，形状切换（矩形 / 圆角 / 椭圆 / 菱形 / 六边形），线宽 / 线型 / 圆角，文字对齐（左 / 中 / 右），加粗 / 斜体 / 下划线 / 删除线，字号滑杆。

  - 连线样式：当前节点到父节点的连线可设置粗细 / 线型（实线 / 虚线 / 点线）/ 箭头开关，SVG marker 自动生成。

  - 页面 Tab：布局密度（紧凑 / 适中 / 宽松）、编号样式（无 / 1.2.3 / A.a.c / Ⅰ.Ⅱ / 一.二.三）、12 套预设主题风格（经典 / 商务 / 彩虹 / 深色 / 亮色 / 田园 / 海洋 / 日落 / 森林 / 水墨 / 樱花 / 糖果），主题网格预览卡片。

- **思维导图拖拽交互**：

  - 鼠标按住中心主题（根节点）拖动可平移整个画布（grab 光标提示）。

  - 鼠标按住非根节点拖动可重新排序到兄弟节点之间，拖动时显示半透明幽灵元素跟随鼠标 + 蓝色插入指示线标记目标位置，松手完成排序（支持 Ctrl+Z 撤销）。

- **思维导图聚焦模式**：Ctrl+\` 切换聚焦模式，仅高亮从根到选中节点的路径及其后代，其余节点变暗。

- **思维导图 Shift+Tab**：将选中节点提升为父节点的同级（变为祖父节点的子节点）。

- **思维导图批量删除子孙**：Ctrl+Delete / Ctrl+Backspace 一键删除选中节点的所有子孙节点。

**v0.21.10**

- **新增「关于 L.Note」弹窗**：顶栏「更多」菜单新增「关于 L.Note」入口，弹出关于弹窗，包含三部分能力：

  - **显示版本号**：打开弹窗时经 `get_version` API 从后端读取当前版本号，浏览器环境回退到前端常量。

  - **检查更新**：点击「检查更新」经 `check_update` API 联网查询最新版本（优先 GitHub Releases API，失败自动回退 Gitee API），实时显示「已是最新版本 / 发现新版本 / 无法连接更新服务器」；发现新版本时同步弹出轻提示。

  - **更新日志链接**：「更新日志」「项目主页」按钮调用系统默认浏览器打开 GitHub 发布页 / 仓库主页（`open_external` API），避免 WebView 内嵌 iframe 的兼容性问题。

**v0.21.9**

- **新增流程图泳道（泳池）**：工具栏一键插入水平泳道 / 纵向泳道 / 双泳道 / 分割线；泳道标题栏可拖动移动、点击重命名、右键删除，节点随泳道移动自动归位并规避重叠。

- **新增流程图交互增强**：空白处拖拽框选多节点（整组移动 / 批量删除）、从节点边缘连接点拖出连线到目标节点松手吸附（实时预览路径）、连线端点可拖动重连、连线形状支持 直线 / 曲线 / 折线 一键切换（折线自动正交绕开障碍物）、Ctrl+Z 撤销画布编辑。

- **新增流程图样式设置面板**：流程图右上角「样式设置」按钮（仅流程图文档显示），选中节点 / 连线 / 泳道后可设置填充、边框、文字颜色（23 色色板）、线宽、线型（实线/虚线/点线）、圆角、加粗、斜体、字号、箭头开关等，实时预览并随文档持久化；多选节点批量应用样式。

- **修复样式设置面板无法关闭的问题**：点击面板外部区域自动关闭，并移除激活态。

- **修复颜色设置不生效的问题**：颜色改为内联样式直接写入，不再受主题 CSS 覆盖。

- **新增 hover 提示**：顶栏图标按钮悬停显示功能说明气泡。

- **变更可视化区右键行为**：流程图可视化区右键不再弹出浏览器默认菜单。

**v0.21.8**

- **新增「设置」面板**：顶栏「更多」菜单新增「设置」，双 Tab 弹窗（通用 / 快捷键）。通用页支持编辑器字体大小滑块（10–26px，实时预览）、行号显示开关、自动换行开关；设置持久化到 localStorage，「恢复默认设置」一键还原。

- **新增快捷键自定义**：17 项快捷键（保存 / 新建 / 查找 / 替换 / 格式化 / 注释 / 折叠 / 展开 / 合并行等）均可点击录制重绑定（Esc 取消，录制须含修饰键）；冲突检测支持 Ctrl/Cmd 别名（如 `Ctrl-F` 与 `Cmd-F` 视为占用），Windows 主修饰键 Ctrl、macOS ⌘ 自动映射。

- **新增文档类型新建快捷键**：新建富文档 `Ctrl+Shift+N`、新建流程图 `Ctrl+Alt+N`、新建思维导图 `Ctrl+Alt+M`、新建思维笔记 `Ctrl+Shift+M`、新建便利贴 `Ctrl+Alt+S`，均可到设置页重新绑定。

**v0.21.7**

- **顶部格式工具栏按文档类型显隐**：B/I/U/删除线/行内代码/引用/列表/表格 格式工具栏仅在打开富文本文档时显示，打开其他格式（纯文本 / 流程图 / 思维导图 / 思维笔记）自动隐藏，顶栏更简洁。

- **修复顶栏右侧按钮左移**：格式工具栏隐藏后，右侧功能按钮（搜索 / 信息面板 / 保存等）不再左移，始终吸附顶栏最右端；格式工具栏改为绝对居中定位，显示 / 隐藏互不干扰。

**v0.21.6**

- **文档地图升级为「真实文本缩略图」**：视口内区域不再只是抽象色块，改为按行渲染文档真实文字（迷你字号直接绘制字符，可辨识段落形状、标题与内容密度）；视口外区域保留行长度色块。文字带与高亮视口框严格对齐，滚动 / 点击拖拽跳转实时同步，超大文档按可见行区间裁剪渲染、首屏不卡顿。

**v0.21.5**

- **PDF 查看器**：文件树双击 /「打开方式」/ 导入菜单可直接打开 `.pdf`。基于 pdf.js 懒加载渲染（首次打开才加载，离线携带 cmaps 与标准字体，中文 CID 不乱码），支持翻页、缩放（Ctrl+滚轮/重置/适应宽度）、文本层选中复制、空白页检测告警；提供「提取为文本」与「复制内容」；PDF 以只读方式登记进侧栏，保存/导出/磁盘写回全链路拦截。

- **Word 文档查看与导入**：双击 `.doc/.docx` 打开模态查看器——`.docx` 经 mammoth 渲染为排版富文本并统计字数，旧版 `.doc` 由内置多格式解析引擎提取文本；支持一键「导入富文档」、「导入 Markdown」与「复制内容」；全程只读。

- **文档地图（小地图）**：编辑器右侧新增仿 VS Code 的 130px 代码小地图（Canvas 按行采样绘制全景缩略图），高亮视口框随滚动实时同步，点击 / 拖拽按比例跳转；顶栏开关状态持久化（仅文本文档显示）。

- **图片查看器升级为图片编辑器**：通过文件关联双击 /「打开方式」传入图片直接进入独立编辑窗口；支持旋转、水平/垂直翻转、裁剪框拖拽、尺寸调整（锁定宽高比 + 高质量重采样 + 锐化）、滤镜（亮度/对比度/饱和度 + 灰度/反色）；撤销/重置、覆盖保存、另存为。

- **文件关联注册脚本**：`register-file-assoc.ps1`（`.bat` 仅为包装）仅写 HKCU，将 `dist\L.Note.exe` 注册为常见图片格式与 `.md/.markdown` 的默认打开程序。

- **JSON 结构折叠**：新增 `json-brace` 折叠（跳过字符串/注释配对，单行紧凑 JSON 也可折叠）与 `json-auto` 全局兜底。

- **块编辑器图片增强**：图片块支持「更换图片」、插入即弹选择器、加载失败自动重试。

- **启动性能优化**：Mermaid（约 2.5MB）、PDF.js、mammoth 三个大库拆分为独立懒加载包，首次使用时才加载，启动时间大幅缩短。

**v0.21.4**

- 打开所在文件夹：文档菜单 / 顶栏菜单一键在系统文件管理器中定位文档（Windows 选中文件、macOS Finder 显示）

- 修复从 Excel/WPS 复制内容粘贴被误识别为图片的问题：粘贴改为文本优先，剪贴板携带文本时优先走文本粘贴，仅纯图片才按图片插入

- 新增选中文本右键菜单「翻译」功能：自动检测语言并中英互译，桌面版通过 worker 线程 + Python 桥接执行，界面不卡顿

**v0.20.44**

- 批量管理：批量删除完成后自动退出批量模式，关闭顶部批量工具栏

- 文档去重：按「完整路径 + 文件名」规范化判断（含正/反斜杠、大小写），重复导入同一文件不再产生重复条目，而是刷新并打开原文档

- 文档信息面板：新增「位置」项，显示文件所在目录（悬停可见完整路径）

- 磁盘文件保存：已有磁盘文件 Ctrl+S 直接覆盖保存；仅首次保存或无路径时才弹「另存为」，保存与另存为彻底分离

- 磁盘文件刷新：重新打开文档、窗口重新聚焦时自动读取磁盘最新内容（外部软件修改后同步）

- 启动优化：修复启动瞬间左侧闪现空白框

- JSON 工具：支持混合文本中仅序列化 JSON 片段（含半截 JSON），并在原文中高亮标记有问题的位置

- 查找/替换：修复弹窗偶发不显示、上一项/下一项导航失灵，弹窗可拖动，导航按钮重新设计

- 文档排序：排序功能增加开关，默认关闭（保持创建顺序稳定）

- 桌面版导入：改用原生文件选择器，导入文件自动关联磁盘路径

## 历史版本说明

- v0.21.10：关于弹窗 / 检查更新 / 更新日志链接

- v0.21.9：流程图泳道 / 框选多选 / 拖拽连线 / 连线形状切换 / 正交绕障 / Ctrl+Z 撤销 / 样式设置面板 / hover 提示

- v0.21.8：设置面板（字体大小 / 行号 / 自动换行）、17 项快捷键自定义、5 项文档类型新建快捷键

- v0.21.7：顶部格式工具栏按文档类型显隐（仅富文本文档显示）、修复顶栏右侧按钮左移

- v0.21.6：文档地图真实文本缩略图（视口内渲染真实文字）

- v0.21.5：PDF 查看器、Word 查看与导入、文档地图（色块版）、图片编辑器、文件关联注册脚本、JSON 结构折叠、启动性能优化

- v0.21.4：打开所在文件夹、Excel 粘贴误判修复、右键翻译

- v0.20.44：批量删除自动退出批量模式、文档去重规范化、文档信息面板「位置」、磁盘文件直接覆盖保存、启动空白框修复、JSON 混合序列化、查找/替换弹窗修复、排序开关、原生导入

- v0.18.x：富文档（思维笔记）块编辑器、飞书式大纲、bubble menu、富文档磁盘保存

- v0.17：保存/另存为分离、编码转换、时间戳工具

