# L.Note（Inkpad）功能列表

> 版本：v0.21.11　|　更新日期：2026-09-02　|　适用：Windows 桌面版（`dist/L.Note.exe`）

本文档完整列出 L.Note 的全部功能，按功能域分组，供产品、研发、测试与用户查阅。

***

## 1. 侧栏导航（飞书风格）

| 功能    | 说明                                               |
| ----- | ------------------------------------------------ |
| 最近    | 显示全部未删除文档，按 置顶 > 今天 > 昨天 > 本周 > 更早 分组            |
| 我的空间  | 全量文档视图                                           |
| 知识库   | 仅显示 Markdown 与富文档类文档                             |
| 收藏    | 仅显示已收藏文档（★ 标记）                                   |
| 回收站   | 显示已软删除文档，支持恢复 / 彻底删除                             |
| 便利贴   | 独立于文档的轻量便签（卡片式），支持颜色 / 置顶 / 定时提醒 / 到期时间 / 删除进回收站 |
| 搜索框   | 实时过滤当前视图文档（标题/标签/集合名匹配，Ctrl+J 聚焦）                |
| 工作区按钮 | 显示版本信息提示                                         |
| 侧栏折叠  | 顶部汉堡按钮动态切换收起/展开（≡▶/≡◀）                           |

## 1.0 定时能力（v0.21.1，便签模块）

| 功能      | 说明                                          |
| ------- | ------------------------------------------- |
| 便利贴定时提醒 | 单次 / 每天 / 每周（勾选星期）/ 每月（指定日期）+ 时分；到点应用内弹提醒弹窗 |
| 提醒防重复   | 同一提醒同一分钟只弹一次；配置随文档索引持久化                     |
| 便利贴到期时间 | 可设/可清到期时间；卡片展示 已到期（红）/ 即将到期（黄）/ 到期时间        |
| 标签过期时间  | 标签右键设置 N 天过期 / 清除；未过期显示 ⏳；到期自动解除文档关联并消失     |

## 1.1 标签区（便签模块）

| 功能   | 说明                                          |
| ---- | ------------------------------------------- |
| 标签区  | 侧栏折叠区域，展示全部标签（名称 + 文档计数），按计数降序              |
| 标签过滤 | 点击标签过滤文档列表，再点取消；文档条目标签胶囊可点击跳转               |
| 编辑标签 | 文档右键 / 三点菜单「编辑标签」：添加、移除、候选标签点击添加            |
| 标签约束 | 每文档 ≤ 20 个，单标签 ≤ 20 字，重名拒绝，随索引持久化           |
| 标签过期 | 标签右键「设置过期时间 / 清除过期时间」；未过期显示 ⏳；到期自动解除文档关联并消失 |

## 1.2 便签集区（便签模块）

| 功能   | 说明                        |
| ---- | ------------------------- |
| 便签集区 | 侧栏折叠区域，展示集合卡片（名称 + 文档数）   |
| 新建集合 | 便签集区「＋」或弹窗内新建，名称 ≤ 20 字   |
| 加入集合 | 文档三点菜单「加入便签集」多选；批量模式下批量加入 |
| 集合过滤 | 点击集合卡片查看集合内文档；再点取消        |
| 集合管理 | 右键集合：重命名 / 删除（仅删分组，不删文档）  |

## 2. 文档管理

### 2.1 新建（底部 FAB）

| 功能     | 说明                 |
| ------ | ------------------ |
| 新建文档   | 创建普通文本文档（Markdown） |
| 新建富文档  | 创建思维笔记富文档（块编辑）     |
| 新建流程图  | 创建 Mermaid 流程图文档   |
| 新建思维导图 | 创建 Mermaid 思维导图文档  |
| 新建思维笔记 | 创建 InkpadNote 富文档  |
| 新建便利贴  | 创建轻量便利贴（卡片式，四色可选）  |
| 打开文件夹  | 通过原生对话框打开本地文件夹     |
| 导入文件   | 导入本地文件为文档          |

### 2.2 打开 / 编辑 / 保存

| 功能      | 说明                                 |
| ------- | ---------------------------------- |
| 打开文档    | 点击列表项打开；行内操作含 创建副本/重命名/收藏/置顶/导出/删除 |
| 保存      | 自动保存到 localStorage（去抖）；Ctrl+S 手动保存 |
| 保存到磁盘   | 顶栏「保存」按钮，写入磁盘文件（pywebview）         |
| 导出      | 导出为文件（下载 Blob）                     |
| 另存为     | 指定文件名另存                            |
| 打开所在文件夹 | 在系统文件管理器中定位当前文档（文档菜单 / 顶栏菜单）       |
| 删除      | 软删除 → 回收站（可恢复）；回收站内支持彻底删除          |

### 2.3 文档组织

| 功能        | 说明                                    |
| --------- | ------------------------------------- |
| 收藏 / 取消收藏 | 三点菜单操作，收藏视图聚合                         |
| 置顶 / 取消置顶 | 三点菜单操作，置顶分组优先展示                       |
| 创建副本      | 一键复制文档                                |
| 重命名       | 弹窗输入新名称（空名拒绝）                         |
| 批量管理      | 批量模式：全选 / 已选计数 / 批量导出 / 批量删除（软删除）/ 退出 |

### 2.4 磁盘文件查看（PDF / Word，v0.21.5）

| 功能       | 说明                                                                                                                             |
| -------- | ------------------------------------------------------------------------------------------------------------------------------ |
| PDF 查看器  | 双击 /「打开方式」/ 导入打开 `.pdf`：pdf.js 懒加载渲染（离线携带 cmaps/标准字体，中文不乱码），翻页 / 缩放 / 文本层选中复制 / 空白页检测；「提取为文本」转 Markdown 新文档、「复制内容」复制全文；只读登记进侧栏 |
| Word 查看器 | 双击打开 `.doc/.docx`：docx 经 mammoth 排版渲染 + 字数统计，旧版 doc 由内置多格式解析引擎提取文本；一键「导入富文档 / 导入 Markdown / 复制内容」；只读保护                         |
| 文件关联注册   | `register-file-assoc.ps1/.bat` 仅写 HKCU，将 L.Note 注册为常见图片格式与 `.md/.markdown` 默认打开程序；双击图片进图片编辑器、非图片文档由主窗口打开                       |

## 3. 编辑器

| 功能        | 说明                                                                                                                                                                                           |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 语法高亮      | 12 种语言：Markdown / JSON / XML / HTML / JavaScript / Python / CSS / SQL / Shell / YAML / C·Java·C++ / Mermaid                                                                                  |
| 括号自动配对    | 输入左括号自动补右括号                                                                                                                                                                                  |
| 自动补全      | 代码提示补全                                                                                                                                                                                       |
| 代码折叠      | 按缩进/语法折叠；支持折叠级别 1\~5、折叠全部、展开全部                                                                                                                                                               |
| JSON 结构折叠 | JSON 按花括号/方括号折叠（自动跳过字符串与注释），单行紧凑 JSON 也可折叠，数组折叠显示元素数「⋯ N 项」                                                                                                                                  |
| 文档地图      | 右侧 130px 代码小地图（仿 VS Code minimap）：全文档按真实行距渲染文本缩略（迷你字号字符，可辨识段落形状与内容密度），内容超过面板高度时地图内部可滚动；高亮视口框随编辑器滚动同步移动并自动带动地图滚动条，拖动地图滚动条/滚轮反向跳转编辑器（双向联动）；点击/拖拽按比例跳转；canvas 只绘制可见切片，超大文档清晰不卡顿；顶栏开关持久化，仅文本文档显示 |
| 多光标       | 按住 Ctrl 点击多处同时编辑                                                                                                                                                                             |
| 行号        | 点击行号选择整行                                                                                                                                                                                     |
| 字体缩放      | Ctrl+滚轮缩放编辑器字体（持久化）                                                                                                                                                                          |
| 撤销 / 重做   | 编辑历史撤销重做                                                                                                                                                                                     |

## 4. 预览与渲染

| 功能            | 说明                                  |
| ------------- | ----------------------------------- |
| Markdown 实时预览 | marked 渲染；工具栏「预览」开关                 |
| 代码高亮          | highlight.js 在预览中高亮代码块              |
| 数学公式          | KaTeX 渲染 `$...$` / `$$...$$`        |
| Mermaid 图表    | 流程图 / 时序图 / 思维导图实时渲染，支持缩放、拖拽平移、双击复位 |
| 预览区交互         | 拖拽平移 / Ctrl+滚轮缩放 / 双击复位             |

## 4.1 流程图交互（v0.21.9）

| 功能       | 说明                                                                                                                                               |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 泳道 / 泳池  | 工具栏支持水平泳道 / 纵向泳道 / 双泳道 / 分割线；泳道标题栏拖动移动、点击重命名、右键删除，节点随泳道移动归位并自动规避重叠；渲染兼容旧文档（默认水平泳道）                                                               |
| 框选多选     | 空白处拖拽框选多个节点，支持整组拖动 / 批量删除；样式面板对多选节点批量应用样式                                                                                                        |
| 拖拽连线     | 从节点边缘连接点拖出连线，拖到目标节点松手吸附，实时预览路径；连线模式下点击两个节点依次连接                                                                                                   |
| 端点重连     | 选中连线后拖动端点，可重新连接到其他节点                                                                                                                             |
| 连线形状     | 选中连线后工具栏一键切换 直线 / 曲线 / 折线；折线支持正交绕障路径自动绕开障碍物                                                                                                      |
| 撤销       | Ctrl+Z 撤销上一步画布编辑操作（模型快照栈，上限 50 步）                                                                                                                |
| 样式设置面板   | 流程图右上角「样式设置」按钮（仅流程图文档显示）：节点（填充 / 边框 / 文字颜色、线宽、线型 实线·虚线·点线、圆角、加粗、斜体、字号）、连线（线条 / 文字颜色、线宽、线型、箭头开关、字号）、泳道（底色 / 标题栏 / 边框 / 标题文字颜色），23 色色板，实时预览并随文档持久化 |
| 面板交互     | 点击按钮开合样式面板并高亮按钮；点击面板外部区域自动关闭面板                                                                                                                   |
| hover 提示 | 顶栏图标按钮悬停显示功能提示气泡（tip-show）                                                                                                                       |
| 右键行为     | 可视化区右键拦截，不弹出浏览器默认菜单                                                                                                                              |

## 4.2 思维导图编辑（v0.21.11）

| 功能       | 说明                                                                                                                                               |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 右键菜单     | 右键节点弹出上下文菜单（添加子节点 / 添加同级 / 编辑 / 删除 / 复制 / 剪切 / 粘贴 / 折叠展开）；空白处右键弹出简化菜单（添加根子节点 / 粘贴）；菜单外点击自动关闭                                                    |
| 复制 / 剪切 / 粘贴 | Ctrl+C / Ctrl+X / Ctrl+V 复制粘贴节点子树；剪贴板同时写入 JSON（跨文档粘贴保留结构）和纯文本（缩进层级 Markdown）；支持跨 mind 实例粘贴                                                       |
| 样式面板（节点 Tab） | 填充 / 边框 / 文字 23 色色板；形状切换（矩形 / 圆角 / 椭圆 / 菱形 / 六边形）；线宽 / 线型 / 圆角；文字对齐（左 / 中 / 右）；加粗 / 斜体 / 下划线 / 删除线；字号滑杆                                            |
| 连线样式     | 当前节点到父节点的连线可设置粗细 / 线型（实线 / 虚线 / 点线）/ 箭头开关；SVG marker 自动生成                                                                                        |
| 样式面板（页面 Tab） | 布局密度（紧凑 / 适中 / 宽松）；编号样式（无 / 1.2.3 / A.a.c / Ⅰ.Ⅱ / 一.二.三）；12 套预设主题风格（经典 / 商务 / 彩虹 / 深色 / 亮色 / 田园 / 海洋 / 日落 / 森林 / 水墨 / 樱花 / 糖果），主题网格预览卡片            |
| 拖拽平移画布   | 鼠标按住中心主题（根节点）拖动可平移整个画布（transform translate，grab 光标提示）                                                                                              |
| 拖拽重排序    | 鼠标按住非根节点拖动可重新排序到兄弟节点之间，拖动时显示半透明幽灵元素跟随鼠标 + 蓝色插入指示线标记目标位置，松手完成排序（支持 Ctrl+Z 撤销）                                                                       |
| 聚焦模式     | Ctrl+` 切换聚焦模式，仅高亮从根到选中节点的路径及其后代，其余节点变暗                                                                                                          |
| Shift+Tab | 将选中节点提升为父节点的同级（变为祖父节点的子节点）                                                                                                                       |
| 批量删除子孙   | Ctrl+Delete / Ctrl+Backspace 一键删除选中节点的所有子孙节点                                                                                                      |

## 5. 富文档（思维笔记）

| 功能               | 说明                                                                                                    |
| ---------------- | ----------------------------------------------------------------------------------------------------- |
| 块编辑              | InkpadNote 富文档，块级组件                                                                                   |
| 左侧大纲             | 飞书式 TOC：标题列表 + 搜索 + 上一个/下一个 + 刷新 + 展开折叠                                                               |
| 选中工具条            | 选中文本浮出 bubble menu 工具条                                                                                |
| 插入块              | 代码块 / 图片 / 表格 / 流程图思维导图 / 图标 / 数学公式 / 标注提示 / 任务列表 / 引用 / 自动目录 / 分割线 / 分栏（两列）                          |
| 表格插入             | 行数（1~~50）× 列数（1~~20），可选首行为表头                                                                          |
| 图片块增强            | 插入图片块直接弹出选择器；图片块可选中「更换图片」；加载失败自动经 `read_file_b64` 以 data URI 重试；富文档目录缓存加速图片解析                         |
| 顶部格式工具栏（v0.21.7） | 顶栏中部 B/I/U/删除线/行内代码/引用/列表/表格 工具栏：仅在打开富文本文档时显示，其他格式（纯文本 / 流程图 / 思维导图 / 思维笔记）自动隐藏；显示 / 隐藏均不影响顶栏右侧功能按钮位置 |

## 5.1 图片查看与编辑（v0.21.5）

| 功能      | 说明                                                                 |
| ------- | ------------------------------------------------------------------ |
| 独立编辑窗口  | 文件关联双击 /「打开方式」传入图片直接进入图片编辑窗口                                       |
| 旋转 / 翻转 | 旋转 90°、水平 / 垂直翻转                                                   |
| 裁剪      | 裁剪框拖拽（含四角手柄）                                                       |
| 尺寸调整    | 锁定宽高比 + 高质量重采样 + unsharp 锐化                                        |
| 滤镜      | 亮度 / 对比度 / 饱和度滑杆 + 灰度 / 反色                                         |
| 保存      | 覆盖保存（save\_image\_file）/ 另存为（save\_image\_as）；撤销 / 重置；退出可放弃修改；窗口缩放 |

## 6. 文本工具

### 6.1 JSON 工具

| 功能         | 说明                 |
| ---------- | ------------------ |
| JSON 格式化   | 2 空格缩进格式化          |
| JSON 压缩    | 压缩去空白              |
| 转义 / 去转义   | 转义引号与换行 / 还原       |
| 复制         | 复制当前内容             |
| 对象转 GET 参数 | JSON 对象转 query 字符串 |

### 6.2 转换工具

| 功能             | 说明             |
| -------------- | -------------- |
| Unicode → 中文   | 解码 `\uXXXX`    |
| 中文 → Unicode   | 编码为 `\uXXXX`   |
| Base64 编码 / 解码 | UTF-8 安全       |
| URL 编码 / 解码    | 文本 URL 编码往返    |
| 时间戳转换          | 时间戳 ↔ 北京时间（弹窗） |
| 编码转换           | 文档编码设置/转换      |

### 6.3 文本工具（子菜单）

| 分组  | 功能                                                    |
| --- | ----------------------------------------------------- |
| 大小写 | 转大写 / 转小写 / 单词首字母大写 / 反转大小写                           |
| 全半角 | 全角转半角 / 半角转全角                                         |
| 删除行 | 删除空行 / 删除重复行 / 删除行头空白 / 删除行尾空白 / 删除整行 / 删除到行头 / 删除到行尾 |
| 行操作 | 行排序 / 按长度排序 / 反转行序 / 合并行 / 重新缩进                       |
| 折叠  | 折叠全部 / 展开全部 / 折叠级别 1\~5                               |
| 其他  | 插入代码段 / 剪贴板历史                                         |

### 6.4 XML

| 功能      | 说明                            |
| ------- | ----------------------------- |
| XML 格式化 | 自动格式化并校验 XML（`< / > XML 格式化`） |

## 7. 查找与替换（仿 EverEdit）

| 功能        | 说明                                  |
| --------- | ----------------------------------- |
| 查找        | 手动触发（Enter / 按钮），非自动                |
| 替换        | 单个替换 / 批量替换（弹窗）                     |
| 正则模式      | 按正则表达式匹配                            |
| 匹配高亮      | 全文高亮全部匹配                            |
| 当前匹配定位    | 醒目样式标识当前匹配，自动滚动到可见区域                |
| 上一个 / 下一个 | ▲/▼ 独立按钮导航，光标同步移动                   |
| 历史记录      | 查找/替换历史下拉复用                         |
| 收藏规则      | 收藏常用查找规则，持久化                        |
| 更多菜单      | 附加功能入口（对齐按钮定位）                      |
| 弹窗交互      | 顶栏 `≪ 查找 / ≪ 替换` 折叠切换；标题栏可拖拽移动并记忆位置 |

## 8. 文件比较

| 功能      | 说明                    |
| ------- | --------------------- |
| 双栏 diff | 行级比较（diff 库）          |
| 本地文件加载  | 支持加载本地文件参与比较          |
| 独立窗口    | `compare.html` 独立比较窗口 |

## 9. 顶栏 / 状态栏

| 功能     | 说明                                                                             |
| ------ | ------------------------------------------------------------------------------ |
| 查找替换入口 | Ctrl+F 打开查找浮层                                                                  |
| 文档信息面板 | 右侧信息卡（可折叠）：JSON 工具 / XML 工具 / 转换工具 / 文件比较 / 编码设置 快捷入口                          |
| 大纲入口   | 富文档大纲开关                                                                        |
| 更多菜单   | 编码转换 / 文件比较 / XML 格式化 / JSON 工具 / 转换工具 / 文本工具 / 插入块 / 切换预览 / 导出文档 / 另存为 / 删除文档 / 设置 / 关于 L.Note |
| 语言选择   | 12 种语言下拉切换                                                                     |
| 状态栏    | 语言 / 字符数 / 光标位置 / 保存状态 / 编辑状态 / 编码 / 右键诊断                                      |

## 9.1 设置面板（v0.21.8）

| 功能     | 说明                                                                                                                                                                                             |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 设置入口   | 顶栏「更多」菜单新增「设置」，弹出双 Tab 设置弹窗（通用 / 快捷键）                                                                                                                                                          |
| 通用设置   | 编辑器字体大小（滑块 10–26px，实时预览，与 Ctrl+滚轮缩放共用存储）、行号显示开关、自动换行开关                                                                                                                                         |
| 设置持久化  | 持久化到 localStorage（`inkpad.settings.v1`）；「恢复默认设置」一键还原；旧 `inkpad.fontsize` 键自动迁移                                                                                                                 |
| 快捷键自定义 | 17 项可配置快捷键（保存 / 新建 / 新建富文档 / 新建流程图 / 新建思维导图 / 新建思维笔记 / 新建便利贴 / 查找 / 替换 / 查找上下一个 / 格式化 / 注释 / 折叠 / 展开 / 选中下一处 / 合并行），点击按键按钮进入录制态（Esc 取消），录制须含至少一个修饰键，冲突检测含 Ctrl/Cmd 别名（`Ctrl-F` 与 `Cmd-F` 视为占用） |
| 跨平台映射  | Windows 主修饰键 Ctrl、macOS ⌘，CodeMirror 键表自动生成互补变体，跨平台生效                                                                                                                                          |

## 9.2 关于 L.Note（v0.21.10）

| 功能     | 说明                                                                          |
| ------ | --------------------------------------------------------------------------- |
| 关于入口   | 顶栏「更多」菜单新增「关于 L.Note」，弹出关于弹窗（应用图标 / 名称 / 版本号 / 简介）                      |
| 显示版本号  | 打开弹窗时经 `get_version` API 从后端读取当前版本号；浏览器环境回退到前端常量                             |
| 检查更新   | 「检查更新」按钮经 `check_update` API 联网查询最新版本（优先 GitHub Releases API，失败自动回退 Gitee API） |
| 更新状态   | 实时显示「已是最新版本 / 发现新版本（含新版本号与发布页）/ 无法连接更新服务器」；发现新版本同步弹出轻提示               |
| 更新日志链接 | 「更新日志」按钮调用系统默认浏览器打开 GitHub Releases 发布页（`open_external` API）                 |
| 项目主页链接 | 「项目主页」按钮调用系统默认浏览器打开 GitHub 仓库主页                                          |

## 10. 右键菜单（编辑器）

| 分组      | 功能                                    |
| ------- | ------------------------------------- |
| 编辑      | 撤销 / 重做 / 剪切 / 复制 / 粘贴 / 全选 / 删除选区    |
| JSON 工具 | JSON 格式化 / 压缩 / 转义 / 去转义 / Unicode↔中文 |
| 行操作     | 合并行 / 行排序 / 重新缩进                      |
| 文本      | 转大写 / 转小写 / 注释切换                      |
| 删除行     | 删除空行 / 删除重复行 / 删除行头空白 / 删除行尾空白 / 删除整行 |
| 折叠      | 折叠全部 / 展开全部                           |

## 11. 右键菜单（文档列表）

| 功能                             | 说明                   |
| ------------------------------ | -------------------- |
| 打开                             | 打开文档                 |
| 创建副本 / 重命名 / 收藏 / 置顶 / 导出 / 删除 | 同三点菜单                |
| 编辑标签 / 加入便签集                   | 便签模块：标签编辑浮层 / 集合选择浮层 |

## 12. 数据与隐私

| 功能   | 说明                                                                                                                                                            |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 本地存储 | 索引 `inkpad.docs.v1` + 正文 `inkpad.content.<id>` + 活动文档 `inkpad.active.v1` + 便签集 `inkpad.collections.v1` + 标签过期 `inkpad.tagmeta.v1` + 文档地图开关 `inkpad.docmap.v1` |
| 磁盘集成 | pywebview 原生文件对话框：打开 / 保存 / 导入                                                                                                                                |
| 隐私   | 无账户、无数据上传；应用运行时不联网，仅用户主动点击「关于 → 检查更新」时查询一次版本信息                                                                                                                                               |

## 13. 快捷键一览

| 快捷键                | 功能          |
| ------------------ | ----------- |
| Ctrl+S             | 保存          |
| Ctrl+F             | 查找/替换浮层     |
| Ctrl+J             | 聚焦侧栏搜索框     |
| Ctrl+.             | 右键菜单兜底触发    |
| Ctrl+滚轮            | 编辑器字体缩放     |
| Menu 键 / Shift+F10 | 打开右键菜单      |
| Esc                | 关闭右键菜单 / 弹窗 |
| Ctrl+N             | 新建文档（纯文本）   |
| Ctrl+Shift+N       | 新建富文档       |
| Ctrl+Alt+N         | 新建流程图       |
| Ctrl+Alt+M         | 新建思维导图      |
| Ctrl+Shift+M       | 新建思维笔记      |
| Ctrl+Alt+S         | 新建便利贴       |

> 以上快捷键（含保存 / 查找 / 替换等共 17 项）均可到「更多 → 设置 → 快捷键」页重新录制自定义，Windows 主修饰键 Ctrl 与 macOS ⌘ 自动映射。新建便利贴命中后自动切换便利贴视图并弹出「已新建便利贴」提示。

